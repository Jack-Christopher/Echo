[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: pure logic tests
    integration: multi-module tests with mocks
    qt: requires pytest-qt
    smoke: real OS/hardware; skipped by default
addopts = -v -m "not smoke"
qt_api = pyside6
